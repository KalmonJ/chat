import { LoginForm } from "components/login/LoginForm";

export default function Home() {
  return (
    <div className="h-screen w-screen flex justify-center items-center bg-group">
      <LoginForm />
    </div>
  );
}
